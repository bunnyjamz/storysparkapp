VITE_SUPABASE_URL=your-supabase-url
VITE_SUPABASE_ANON_KEY=your-supabase-anon-key

# Server-side only (Vercel Functions / Edge Runtime)
# Configure your OpenAI provider inside Vercel → Project Settings → AI Gateway
# and Vercel will generate this token for your project.
VERCEL_AI_GATEWAY_TOKEN=your-vercel-ai-gateway-token

# Optional (only needed if you are NOT configuring OpenAI inside Vercel AI Gateway)
OPENAI_API_KEY=sk-your-openai-key
